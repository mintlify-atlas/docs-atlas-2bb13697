---
title: "DBOO Data Model: Object Graph Overview"
sidebarTitle: "Overview"
description: "Survey of all domain entities in DBOO and how they relate as an object graph persisted to a db4o .yap file."
---

DBOO stores all application data as a connected object graph inside a single db4o `.yap` file. Rather than relying on traditional relational foreign-key joins, objects hold direct references or store string codes (acting as primary/foreign key identifiers) to link related objects. This approach leverages db4o's native object persistence while remaining compatible with Java Swing's GUI table-binding patterns.

## Entity inventory

<CardGroup cols={2}>
  <Card title="Persona" href="/reference/persona">
    Base person class that provides shared personal data fields — cedula, nombre, apellido, correo, and more — inherited by every user-facing entity.
  </Card>
  <Card title="Organizador" href="/reference/organizador">
    Event organizer with login credentials (`usuario` / `contraseña`) and a budget (`presupuesto`). Extends `Persona`.
  </Card>
  <Card title="Comerciantes" href="/reference/comerciantes">
    Merchant participant assigned to a commercial booth. Stores trade type, products, and services. Extends `Persona`.
  </Card>
  <Card title="Personal" href="/reference/personal">
    Contracted staff member with a role, department, performance rating, and contract date range. Extends `Persona`.
  </Card>
  <Card title="Patrocinador" href="/reference/patrocinador">
    Event sponsor with description, contact, and social media links. Links back to a `Persona` record via `cedula_perso`.
  </Card>
  <Card title="Publico" href="/reference/publico">
    Public attendee with ticket type, registration status, preferences, and payment amount. Holds FK codes for surveys, images, ratings, and comments.
  </Card>
  <Card title="Ciudad / Direccion / Departamento" href="/reference/location-classes">
    Geographic and organizational location entities that capture city, street address, and department data for events and participants.
  </Card>
  <Card title="Puesto" href="/reference/location-classes">
    Commercial booth entity assigned to a merchant (`Comerciantes`) through the `codigo_puesto` foreign key field.
  </Card>
</CardGroup>

## Relationship summary

The table below shows how each entity connects to others through its key fields. In a pure db4o object graph these would typically be direct object references; the DBOO codebase uses string codes as an alternative identifier approach that keeps GUI table binding straightforward.

| Entity | Key field | Related to | Via |
|---|---|---|---|
| `Organizador` | `cod_organizador` | `Evento` | `cod_organizador` FK on Evento |
| `Comerciantes` | `codigo_participante` | `Puesto` | `codigo_puesto` FK on Comerciantes |
| `Personal` | `codigo_perso` | `Evento` | `cod_evento` FK on Personal |
| `Publico` | `codigo_publico` | `Encuesta`, `Imagen` | `codigo_encuesta`, `codigo_sube_ima` FKs |
| `Patrocinador` | `codigo_patri` | `Persona` | `cedula_perso` FK on Patrocinador |

<Note>
  All foreign key codes are stored as `String` fields and resolved at query time using db4o's native object query (`ObjectSet`). There are no explicit join tables or relational schema files — the object graph is the schema.
</Note>
